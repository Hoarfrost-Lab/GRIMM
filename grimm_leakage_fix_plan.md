# GRIMM Data-Split Leakage Fix — Implementation Plan

**Branch:** `fix/data-split-leakage` (in `~/Projects/GRIMM`, cloned from `github.com/Hoarfrost-Lab/grimm`)
**Date:** 2026-06-15
**Goal:** Fix the data-split leakage bugs, re-deploy a corrected dataset, and prepare the GRIMM paper for resubmission.

## Decisions (settled with PI)

1. **Dual-track strategy:**
   - **Patch v1 now** — re-upload a decontaminated **test-2** only; keep deployed **train / test-1 / valid byte-identical** so the two already-submitted downstream papers' underlying data is untouched and their citations stand.
   - **Build a clean v2** alongside — full A–F fix, becomes the recommended go-forward benchmark.
2. **The ~6% AA test-1 sequence overlap (Bug B/C) in v1 is acceptable for resubmission and will be disclosed** in the paper's methods/limitations (not fixed in v1).
3. **v1 test-2 is decontaminated to be FULLY DISJOINT** from train, valid, AND test-1 (exact accession+sequence match).
4. **v2 is a fresh GRIMM-scoped regeneration**, NOT a reuse of the extensions/TEACUP datasets (which changed scope and conventions). 50% identity only, **reusing the existing UniRef50 cluster IDs** (no re-clustering).
5. **v2 ships in the same HF repo** (`HoarfrostLab/GRIMM`). Work stays on branch `fix/data-split-leakage`; merge to `main` only after all uploads are done and the postdoc signs off.
6. **HF auth:** token cached at `~/.cache/huggingface/token` (use `huggingface_hub` login).

## Bugs found (verified against the published HF dataset)

| ID | Bug | Location | Measured impact | Fixed in v1? | Fixed in v2? |
|----|-----|----------|-----------------|--------------|--------------|
| **A** | test-2 file written from `orphans` (all) instead of the `test2` sample; 80% of orphans also go to train | `create_data_splits_custom.py:111` (+ design at `:90`) | AA: 80.0% of test-2 rows share accession w/ train, 83.6% share sequence. Nuc: 80.3%. | ✅ (decontaminate test-2) | ✅ |
| **B** | ECs with 1–2 UniRef50 clusters split by **row**, not by cluster → same cluster in train & test-1/valid | `create_data_splits_custom.py:70-80` | AA: 5.8% of test-1 rows, 4.1% valid have exact seq in train. Nuc: 0.4% / 0.2%. | ❌ (disclosed) | ✅ |
| **C** | AA `Sequence` = UniRef50 **representative**, not each protein's own sequence | `combine_data.py:45` | Amplifies B ~15× in AA vs nuc | ❌ (disclosed) | ✅ (real per-protein seq) |
| **D** | 5 "folds" not independent — `groupby` iterates clusters deterministically; `shuffle` doesn't change cluster→partition assignment | `create_data_splits_custom.py:50` | test-1 ~95% identical across folds; test-2 100% identical | ❌ (disclosed) | ✅ (shuffled per fold) |
| **E** | nuc split script never resets `entries/ecs/seqs` → would make valid/test1/test2 cumulative supersets | `create_nuc_splits.py:27-47` | Does NOT match deployed nuc data → repo code ≠ what generated v1 | ✅ (code fix) | ✅ |
| **F** | declared `SEED` never passed to `shuffle()`/`sample()` → deployed v1 not byte-reproducible from script | `create_data_splits_*.py` | v1 splits can't be exactly regenerated by re-running | ✅ (code fix; provenance note) | ✅ (seeded) |

**Reproducibility note:** because of Bug F, we do **not** regenerate v1 by re-running the script. The deployed CSVs are the canonical v1 artifacts; the v1 test-2 patch is produced by operating **directly on the deployed files**.

## Workstream 1 — v1 test-2 decontamination patch (deploy)

Operate on the deployed dataset (canonical source: HF `HoarfrostLab/GRIMM`; local cache: `~/Projects/TEACUP/data/external_benchmarks/GRIMM-base/hf_dataset/EC/`).

- For each modality (`amino_acids`, `nucleotides`) × split (1–5):
  - `patched_test2 = test2 rows whose Entry ∉ train AND Sequence ∉ train` (exact-match, reviewer-defensible OOD — mirrors `TEACUP .../evaluation/decontaminate.py`).
- **Verify:** post-patch overlap with train = 0 by both `Entry` and `Sequence`; also report overlap with valid/test-1; report new test-2 sizes (expect ~420 AA / ~820 nuc per fold from ~2,104 / ~4,162); sanity-check that retained test-2 EC numbers behave as OOD.
- **Deploy:** clone HF dataset repo, replace only the `test2.csv` files, commit with a clear message, push. *(Requires `HF_TOKEN` + explicit go-ahead.)*
- **Dataset card:** add a changelog note on HF documenting the v1 test-2 correction.

## Workstream 2 — repo code fixes (correctness + provenance)

Leave correct, runnable code in the repo (does **not** regenerate v1):
- `create_data_splits_custom.py:111`: write `test2`, not `orphans` (Bug A).
- Add `random_state=SEED` to `shuffle(df)` and `.sample()` (Bug F).
- `create_nuc_splits.py`: reset `entries/ecs/seqs` per file (Bug E).
- README: note this script reproduces v1 *behavior*; for leakage-free splits use the v2 engine. Document Bug F (v1 used unseeded RNG; deployed CSVs are canonical).

## Workstream 3 — clean v2 dataset (fresh GRIMM-scoped regeneration)

Fresh regen at **50% identity reusing existing UniRef50 IDs** (no re-clustering, no TEACUP/extensions scope changes). Borrow the *algorithm* from `~/Projects/TEACUP/src/teacup/splitting_engine.py` (by-cluster, shuffled folds, seeded, QC) but keep **original GRIMM scope/conventions** — notably **do NOT expand multi-EC rows** (keep compound EC labels as v1 did), so v2 is a faithful fixed-v1, not a re-scoped dataset.

Fixes by construction: A (correct test2), B (split by cluster), D (shuffle cluster→partition per fold), E (nuc per-file), F (seeded).

**Data acquisition (none of the source files are local):**
- Reconstruct the base table (Entry, EC number, EMBL, RefSeq, UniRef50/90/100) from the **union of v1 deployed partitions** → preserves "existing UniRef50 IDs" with no download.
- **[OPEN — Bug C] AA sequences:** real per-protein sequences require downloading `uniprot_sprot.fasta` (pinned to the 2025-05-23 release) and mapping Entry→sequence. Alternative: keep UniRef50 representative sequences (no download), which is leakage-free once B/D are fixed but retains the "duplicate input, possibly different label" artifact.
- Nucleotides: reconstruct Entry→CDS mapping from v1 nucleotide partitions, re-split with the fixed nuc logic.

**QC:** cluster-exclusivity per label; 0 leakage across all partitions by accession & sequence; confirm folds are independent.

**Hosting:** same HF repo `HoarfrostLab/GRIMM`, under a versioned path (e.g. `EC_v2/...` alongside the existing `EC/...`).

## Workstream 4 — paper disclosure (resubmission)

- Methods/limitations sentence: v1 train/test-1 carry ~6% AA exact-sequence duplication (consequence of UniRef50-representative-sequence convention, Bug C); test-2 corrected to true OOD; v2 is fully leakage-free.
- Describe v2 as the clean benchmark; reference v1 for prior-work comparability.

## Progress log

- ✅ Deep review + live verification (HF `42b52d6`, GitHub `b26a8ce`).
- ✅ WS1 v1 test-2 patch — fully disjoint, 0 residual leakage, staged `data_patch/v1_test2/`.
- ✅ WS2 v1 code fixes (A/E/F).
- ✅ v2 split structure (`create_data_splits_v2.py`) — QC: 0 within-label cluster splits, 0 dup accessions, 0 test2-in-train, independent folds.
- ✅ v2 AA dataset — release-2025_02 sequences (100% coverage), **0 (Sequence,EC) leakage**; in `v2_build/data/GRIMM_v2/amino_acids/`.
- ✅ Re-pinned to release-2025_02 (`extract_release_2025_02.py`) — drift resolved at root (guard now drops 0 rows).
- ✅ v2 nucleotides (`create_nuc_splits_v2.py`) — 0 leakage; in `v2_build/data/GRIMM_v2/nucleotides/`.
- ✅ Final QC (both modalities, 5 folds): 0 leakage, 0 test-2-in-train, independent folds.
- ✅ v1 disclosure draft (`data_patch/v1_dataset_card_disclosure.md`, PI-edited).
- ✅ EC-label whitespace bug found+fixed (normalize in `combine_data.py`); 283 spuriously-split labels merged; v2 regenerated.
- ✅ Scripts consolidated to original names (`combine_data.py`, `create_data_splits_custom.py`, `create_nuc_splits.py`); `_v2` files removed (in git history).
- ✅ Multi-EC handling confirmed = v1 (compound label strings kept distinct, no expansion).
- ✅ Cross-label overlap decision: KEEP (per-label design; 0 same-label leakage). [PI]
- ✅ v2 dataset card (`data_patch/hf_dataset_card.md`) + GitHub README updated.
- ✅ Deploy script `push_to_hub.py` (dry-run default) — verified dry-run.
- ⏸️ **PAUSED before HF push** (needs `--execute` + PI go). Then postdoc sign-off → merge branch to main.

### ⚠️ Flag for PI before push
Renaming live `EC/` → `EC_v1/` on HF will break any external code loading the `EC/` path from the Hub (the two submitted papers + TEACUP/SeqLengthvsFn use LOCAL caches, so they're safe). Options: proceed with rename (as requested), or also keep an `EC/` copy pointing at v1.

## Git / deployment workflow

1. Commit on `fix/data-split-leakage`: code fixes (WS2), decontamination script + verification (WS1), this plan, docs.
2. Patch + push v1 test-2 to HF (WS1 deploy) — explicit go.
3. Build, QC, publish v2 (WS3) — explicit go.
4. Merge `fix/data-split-leakage` → `main` when satisfied.

## Key reframing — B and C are deviations from the PAPER, not design choices

The preprint (arXiv 2602.16504v1) documents the *correct* method: per-protein **SwissProt sequences** (UniRef50 used only for stratification), 2-cluster labels split **by cluster** (1 train / 1 test-1), singleton-cluster labels are orphans. The **released data does not implement this**:
- AA `Sequence` is the **UniRef50 representative** (verified live: distinct clusters == distinct sequences == 48,966; one 196-accession cluster has 1 sequence) — Bug C.
- Low-support handling keys on **row count** and splits **by row**, not by cluster — Bug B.

So fixing B + C in v2 = making the data finally match the paper. **Both submitted papers used the AA splits**, so they inherit both issues → v1 README must disclose them and steer users to v2.

## Workstream 5 — v1 dataset README / card (disclosure)

Add to the HF dataset card: (1) test-2 corrected to true OOD (v1 patch); (2) **known issues in v1 train/test-1** — AA sequences are UniRef50 representatives rather than per-protein SwissProt sequences (Bug C), and ~6% of AA test-1 rows share a cluster/sequence with train (Bug B); (3) recommend **v2** for new work. Keep v1 static for citation stability.

### Resolved decisions
- v2 = fresh GRIMM-scoped regen, 50% only, existing UniRef50 IDs, **no multi-EC expansion**. ✓
- v2 **fixes Bug C → real per-protein SwissProt sequences** (pinned 2025-05 release) and **Bug B → by-cluster** low-support handling, matching the paper. ✓
- v2 hosting = same HF repo, versioned path. ✓
- v1 train/test-1/valid stay **static**; test-2 patched to fully disjoint. ✓ (done, verified)
- Live HF (`42b52d6`) == cache; GitHub (`b26a8ce`) == reviewed code. ✓
- HF token at `~/.cache/huggingface/token`. ✓
- Base table: reconstruct from union of v1 partitions (keeps exact UniRef50 IDs); fetch only real sequences anew. ✓
